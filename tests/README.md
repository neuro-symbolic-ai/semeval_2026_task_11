# Tests (`tests/`)

This directory contains **unit and integration tests** for the project, with a focus on correctness, determinism, and reproducibility.

Testing is especially important for symbolic components, where subtle bugs can invalidate results.

---

## Directory Structure