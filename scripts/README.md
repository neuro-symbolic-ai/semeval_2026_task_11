# Scripts (`scripts/`)

This directory contains **executable helper scripts** that orchestrate preprocessing, abstraction, and symbolic reasoning pipelines.

Scripts are intended to be run from the **project root** and act as thin wrappers around functionality implemented in `src/`.

---

## Directory Structure